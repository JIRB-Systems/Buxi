import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cr.buxi.app',
  appName: 'Buxi',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  }
};

export default config;
